<div class="loading">
  <div class="animation">ご安心ください！</div>
</div>

<div class="title_view new_title mt-4 ml-5">パスワードをお忘れですか？</div>
<span class="under_border_title_new new_under mt-4 ml-5"></span>

<div class="post_index_title new_sub_title " >再設定手続きへ ↓</div>
<span class="under_border_subtitle new_sub_under"></span>

<div class="form_area new_area">
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%#= render "customers/shared/error_messages", resource: resource %>
    <div class="form-group mt-4">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"  %>
    </div>
    <div class="form-group text-center mt-5">
      <%= f.submit "パスワードの再設定方法を送信する", class: 'btn btn-warning form_btn'  %>
    </div>
    <div class="mb-5 mt-5">
      <%= render "customers/shared/links" %>
    </div>
  <% end %>
</div>